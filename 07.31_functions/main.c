#include <stdio.h>

/* To include the information in "add.h": */
#include "add.h"

int main(void) {
    /* To call a function that takes two ints and returns an int: */
    printf("%d\n", add(1, 2));

    return 0;
}
